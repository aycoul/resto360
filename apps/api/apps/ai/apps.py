from django.apps import AppConfig


class AIConfig(AppConfig):
    """Configuration for the AI app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.ai"
    verbose_name = "AI Menu Builder"
