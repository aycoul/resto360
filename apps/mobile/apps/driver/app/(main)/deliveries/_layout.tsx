/**
 * Deliveries stack layout.
 */
import { Stack } from 'expo-router';

export default function DeliveriesLayout() {
  return (
    <Stack
      screenOptions={{
        headerShown: true,
        headerTintColor: '#4CAF50',
      }}
    />
  );
}
